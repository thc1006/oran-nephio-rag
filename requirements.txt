# O-RAN × Nephio RAG System Dependencies - FIXED VERSION
# Uses binary packages to avoid compilation issues

# Core web and async libraries (no compilation needed)
requests>=2.28.0,<3.0.0
aiohttp>=3.8.0,<4.0.0
beautifulsoup4>=4.11.0,<5.0.0
lxml>=4.9.0,<5.0.0

# Environment and configuration management  
python-dotenv>=1.0.0,<2.0.0
pydantic>=2.4.0,<3.0.0
pydantic-settings>=2.4.0,<3.0.0

# Data processing (use binary wheels)
# Removed problematic numpy dependency - will use lighter alternatives
pandas>=2.0.0,<3.0.0  # Includes numpy as binary dependency

# Vector database - ChromaDB with minimal deps
chromadb>=0.4.0,<0.6.0

# For embeddings - use a lighter alternative to sentence-transformers
# Will use OpenAI-compatible embeddings through Puter.js instead

# LangChain minimal (WITHOUT anthropic direct integration)
langchain-core>=0.1.0,<0.4.0
langchain-community>=0.0.20,<0.4.0
# REMOVED: langchain-anthropic (violates Puter.js constraint)
# REMOVED: langchain-huggingface (heavy dependency)

# Text processing
nltk>=3.8,<4.0.0
scikit-learn>=1.3.0,<1.6.0  # For TfidfVectorizer and cosine similarity

# Scheduling and automation
schedule>=1.2.0,<2.0.0

# User interface enhancements
rich>=13.0.0,<14.0.0
tqdm>=4.64.0,<5.0.0

# System tools (lightweight)
psutil>=5.9.0,<6.0.0
python-dateutil>=2.8.0,<3.0.0

# JSON and data processing
jsonschema>=4.19.0,<5.0.0

# Network security
urllib3>=1.26.0,<3.0.0
certifi>=2023.0.0

# Development and testing tools
pytest>=7.4.0,<9.0.0
pytest-asyncio>=0.21.0,<1.0.0
pytest-cov>=4.1.0,<6.0.0
pytest-mock>=3.10.0,<4.0.0
responses>=0.23.0,<1.0.0

# Code quality tools
black>=23.0.0,<25.0.0
flake8>=6.0.0,<7.0.0
isort>=5.12.0,<6.0.0

# Browser automation for Puter.js integration
selenium>=4.0.0,<5.0.0
webdriver-manager>=3.8.0,<5.0.0

# Monitoring (optional - lightweight)
prometheus-client>=0.17.0,<1.0.0

# Memory profiling (lightweight)
memory-profiler>=0.60.0,<1.0.0